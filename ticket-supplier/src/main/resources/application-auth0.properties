# Level 2 'auth0' profile: every request must carry the broker's M2M JWT (see
# SecurityConfig). Env-var driven; a missing var fails at startup instead of half-working.
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://${AUTH0_DOMAIN}/
auth0.api.audience=${AUTH0_API_AUDIENCE}
