# Level 2 'async' profile: orders are queued and completed in the background
# (OrderQueueListener). The queue is an embedded, in-JVM ActiveMQ Artemis broker,
# so this profile needs no external service or env vars.
spring.artemis.mode=embedded
